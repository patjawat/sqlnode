type Product{
    id:ID!
    name:String
    price:String
}

type Query{
    allProduct:[Product!]
    getProduct(id: Int!): Product
    updateProduct(id: Int!): Product
}

type Mutation{
    createProduct(name:String!, price:String!):Product!
}